import React from 'react';
import ReactDOM from 'react-dom/client';
import LioApp from './LioApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LioApp />
  </React.StrictMode>
);
